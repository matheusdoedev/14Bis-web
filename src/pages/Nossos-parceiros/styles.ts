import styled from "styled-components";

export const LogoInovativa = styled.a`
  & {
    img {
      margin: 0 auto;
      width: 168px;
      height: 85px;
    }
  }
`;

export const LinkInovativa = styled.a`
  & {
    font-size: 12px;
    font-weight: 400;
    color: var(--gray);
    text-decoration: underline;
  }
`;
