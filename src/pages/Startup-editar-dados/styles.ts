import styled from "styled-components";
// components
import { Link } from "react-router-dom";

export const StartupEditarDadosContainer = styled.main`
  & {
  }
`;

export const StartupLogo = styled.div`
  & {
    width: 121px;
    height: 113px;
    border-radius: 33px;
    background: var(--gray);
    margin: var(--gap-md) 0;
  }
`;

export const EditarDadosForm = styled.form`
  & {
    width: 90%;
    margin-bottom: var(--gap-sm);
  }
`;
