import styled from "styled-components";

export const NotificacoesContainer = styled.main`
  & {
    header {
      margin-bottom: var(--gap-md);
    }

    h1 {
      margin-bottom: var(--gap-lg);
    }

    a {
      font-size: 18px;
    }
  }
`;
